# Core runtime dependencies (offline/heuristic mode works with just these)
pydantic>=2.5
pydantic-settings>=2.1
numpy>=1.24
pandas>=2.0
rich>=13.0
typer>=0.9
python-dotenv>=1.0

# Optional: live market data
# yfinance>=0.2.40

# Optional: LLM providers (only needed for live LLM reasoning)
# openai>=1.30
# anthropic>=0.34

# Optional: web dashboard + REST API (`pip install trading-agents[web]`)
# fastapi>=0.110
# uvicorn[standard]>=0.29

# Optional: architecture PDF generation (`pip install trading-agents[docs]`)
# reportlab>=4.0
